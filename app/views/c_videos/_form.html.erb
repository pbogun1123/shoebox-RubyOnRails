<%= form_for(@c_video) do |f| %>
  <% if @c_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c_video.errors.count, "error") %> prohibited this c_video from being saved:</h2>

      <ul>
      <% @c_video.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Client:<br/>
    <%= f.select :client_id, @client_choice %>
  </div>
  
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
